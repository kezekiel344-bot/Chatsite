export function timeAgo(date?: Date | null) { if (!date) return ''; const seconds = Math.floor((Date.now() - date.getTime()) / 1000); const intervals: [number, string][] = [[31536000, 'y'], [2592000, 'mo'], [86400, 'd'], [3600, 'h'], [60, 'm'], [1, 's']]; for (const [sec, label] of intervals) { const val = Math.floor(seconds / sec); if (val > 0) return `${val}${label}`; } return 'now'; }